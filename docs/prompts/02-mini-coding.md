# Codex task template — Slice 3/4

Read `AGENTS.md`, `docs/02-architecture.md`, `.agents/skills/mini-coding/SKILL.md`, `docs/06-acceptance-qa.md`. Implement Python Pyodide Worker OR HTML/CSS/JS iframe on a focused branch (one adapter per PR). Real Run/Stop/Reset, explicit loading/errors; editor fixed files. Test Python stdout/error/infinite-stop or Web DOM/CSS/JS and isolation. Do not implement arbitrary shell, cloud runtime, fake stdout, insecure iframe flags. Show test logs and known browser support.
